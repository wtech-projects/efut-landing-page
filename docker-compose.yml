#1
version: '3.8'
services:
  landing-page:
    image: ghcr.io/wtech-projects/efut-landing-page:master
    restart: always
    networks:
      - cb_network
    
networks:
  cb_network:
    external: true
